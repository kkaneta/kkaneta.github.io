public/
node_modules/
.hugo_build.lock
resources/_gen/
.DS_Store

# Regenerated on every build. Tailwind reads the templates directly, so
# committing this only produced a diff in every pull request.
hugo_stats.json

# Superseded tooling, kept locally for reference.
archive/

# Working notes, not part of the site.
site_spec_v2.md
